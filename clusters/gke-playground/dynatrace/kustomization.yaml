apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dynatrace
resources:
  - dynakube.yaml
# The token Secret is created out-of-band (see ../setup.md) and is NOT managed
# here. If you later adopt Sealed Secrets / External Secrets, add it as a resource.
