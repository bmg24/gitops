# TEMPLATE ONLY — do not put real tokens here and do not commit a filled-in copy.
# Preferred: create the Secret with kubectl (see ../setup.md), so tokens never
# touch Git. This file just documents the shape the operator expects.
#
# Copy to secret.yaml, fill in ROTATED tokens, and `kubectl apply -f secret.yaml`
# (secret.yaml is gitignored).
apiVersion: v1
kind: Secret
metadata:
  name: brian-goins-playground
  namespace: dynatrace
  labels:
    dynatrace.com/created-by: "dynatrace.kubernetes"
type: Opaque
stringData:
  apiToken: "dt0s16.REPLACE_WITH_ROTATED_API_TOKEN"
  dataIngestToken: "dt0s16.REPLACE_WITH_ROTATED_DATA_INGEST_TOKEN"
