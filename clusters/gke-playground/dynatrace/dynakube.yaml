# DynaKube custom resource for the GKE playground cluster.
# Single full-stack DynaKube: cloudNativeFullStack OneAgent + one ActiveGate
# carrying all capabilities (kubernetes-monitoring, routing, debugging).
# NOTE: the token Secret (named "brian-obs-playground") is intentionally NOT
# in this repo. Create it by hand — see ../setup.md. ArgoCD applies only this CR.
---
apiVersion: dynatrace.com/v1beta6
kind: DynaKube
metadata:
  name: brian-obs-playground
  namespace: dynatrace
  labels:
    dynatrace.com/created-by: "dynatrace.kubernetes"
# https://docs.dynatrace.com/docs/ingest-from/setup-on-k8s/reference/dynakube-parameters
spec:
  apiUrl: https://anr47691.sprint.dynatracelabs.com/api
  tokens: brian-obs-playground
  networkZone: gke
  oneAgent:
    hostGroup: gke
    cloudNativeFullStack:
      tolerations:
        - effect: NoSchedule
          key: node-role.kubernetes.io/master
          operator: Exists
        - effect: NoSchedule
          key: node-role.kubernetes.io/control-plane
          operator: Exists
  activeGate:
    capabilities:
      - kubernetes-monitoring
      - routing
      - debugging
    resources:
      requests:
        cpu: 1000m
        memory: 10Gi
      limits:
        cpu: 2000m
        memory: 10Gi
    replicas: 1
  templates:
    extensionExecutionController:
      imageRef:
        repository: anr47691.sprint.dynatracelabs.com/linux/dynatrace-eec
        tag: latest
    otelCollector:
      imageRef:
        repository: public.ecr.aws/dynatrace/dynatrace-otel-collector
        tag: latest
  logMonitoring: {}
  telemetryIngest:
    protocols:
      - jaeger
      - otlp
      - statsd
      - zipkin
    serviceName: telemetry-ingest
  extensions:
    prometheus: {}
